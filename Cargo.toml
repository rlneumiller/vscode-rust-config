[package]
name = "rust-vscode-workspace-configurator"
version = "0.2.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
cargo_metadata = "0.23"

